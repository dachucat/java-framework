<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper  namespace="course.linkflower.link.oneframework.dao.CarPatternMapper">
    <insert id="add" parameterType="course.linkflower.link.oneframework.model.CarPattern" useGeneratedKeys="true" keyProperty="id">
        insert into car_pattern(car_brand_id, car_pattern, time_pattern, pattern_key)
        values(#{carBrandId},#{carPattern},#{timePattern},#{patternKey})
    </insert>

    <select id="countBrandPatternTimeKey" resultType="int">
        select count(*) from car_pattern
        where car_brand_id=#{carBrandId} and car_pattern=#{carPattern} and time_pattern=#{timePattern} and pattern_key=#{patternKey} limit 1
    </select>

    <delete id="deleteById">
        delete from car_pattern where id=#{id}
    </delete>

    <select id="getCarPatternById" resultType="course.linkflower.link.oneframework.vo.carpattern.CarPatternVo">
        select * from car_pattern where id=#{id}
    </select>

    <update id="updateById">
        update car_pattern set car_pattern=#{carPattern}, car_brand_id=#{carBrandId}, time_pattern=#{timePattern},
        pattern_key=#{PatternKey} where id=#{id}
    </update>

    <select id="countBrandPatternTimeKeyDiffId" resultType="int">
        select count(*) from car_pattern
        where car_brand_id=#{carBrandId} and car_pattern=#{carPattern} and time_pattern=#{timePattern} and pattern_key=#{patternKey} and id!=#{id}
        limit 1
    </select>

    <select id="listCarPatternByCarBrandId" resultType="course.linkflower.link.oneframework.vo.carpattern.CarPatternVo">
        select * from car_pattern where car_brand_id=#{carBrandId} limit #{defaultMaxCountLimit}
    </select>
</mapper>