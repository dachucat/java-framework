<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="course.linkflower.link.oneframework.dao.UsedCarAttribMapper">
    <insert id="add" parameterType="course.linkflower.link.oneframework.model.UsedCarAttrib" useGeneratedKeys="true" keyProperty="id">
        insert into used_car_attrib(used_car_id, attrib_key, value, remark, icon, value_type_key, introduction)
        values(#{usedCarId},#{attribKey},#{value},#{remark},#{icon},#{valueTypeKey},#{introduction})
    </insert>

    <delete id="deleteById">
        delete from used_car_attrib where id=#{id}
    </delete>

    <update id="updateById">
        update used_car_attrib set
        <if test="usedCarId">used_car_id=#{usedCarId}</if>
        <if test="attribKey">, attrib_key=#{attribKey}</if>
        <if test="value">, value=#{value}</if>
        <if test="remark">, remark=#{remark}</if>
        <if test="icon">icon=#{icon}, </if>
        <if test="valueTypeKey">, value_type_key=#{valueTypeKey}</if>
        <if test="introduction">, introduction=#{introduction}</if>
        where id=#{id}
    </update>

    <select id="getUsedCarAttribById" resultType="course.linkflower.link.oneframework.vo.usedcarattrib.UsedCarAttribVo">
        select * from used_car_attrib where id=#{id}
    </select>

    <select id="search" resultType="course.linkflower.link.oneframework.vo.usedcarattrib.UsedCarAttribVo">
        select * from used_car_attrib order by id limit #{startCount}, #{pageSize}
    </select>

    <select id="countUsedCarIdAttribKeyValue" resultType="int">
        select count(*) from used_car_attrib where used_car_id=#{usedCarId} and attrib_key=#{attribKey} and value=#{value}
    </select>

    <select id="countUsedCarIdAttribKeyValueDiffId" resultType="int">
        select count(*) from used_car_attrib where used_car_id=#{usedCarId} and attrib_key=#{attribKey} and value=#{value} and id!=#{id}
    </select>

    <select id="countAll" resultType="long">
        select count(*) from used_car_attrib
    </select>

    <select id="countUsedCarIdAttribKey" resultType="int">
        select count(*) from used_car_attrib where used_car_id=#{usedCarId} and attrib_key=#{attribKey}
    </select>

    <select id="countUsedCarIdAttribKeyDiffId" resultType="int">
        select count(*) from used_car_attrib where used_car_id=#{usedCarId} and attrib_key=#{attribKey} and id!=#{id}
    </select>
</mapper>