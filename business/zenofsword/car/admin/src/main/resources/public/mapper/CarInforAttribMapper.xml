<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="course.linkflower.link.oneframework.dao.CarInforAttribMapper">
    <select id="countCarInforIdTypeKey" resultType="int">
        select count(*) from car_infor_attrib where car_infor_id=#{carInforId} and type_car_dict_key={typeCarDictKey}
    </select>

    <insert id="add">
        insert into car_infor_attrib(car_infor_id, type_car_dict_key, value, remark, icon, value_type_car_dict_key, introduction)
        values(#{carInforId},#{typeCarDictKey},#{value},#{remark},#{icon},#{valueTypeCarDictKey},#{introduction})
    </insert>

    <delete id="deleteById">
        delete from car_infor_attrib where id=#{id} limit 1
    </delete>

    <update id="updateById">
        update car_infor_attrib set
        <if test="#{carInforId}!=null">
            car_infor_id=#{carInforId},
        </if>
        <if test="#{typeCarDictKey}!=null">
            type_car_dictKey=#{typeCarDictKey},
        </if>
        <if test="#{value}!=null">
            value=#{value},
        </if>
        <if test="#{remark}!=null">
            remark=#{remark},
        </if>
        <if test="#{icon}!=null">
            icon=#{icon},
        </if>
        <if test="#{valueTypeCarDictKey}!=null">
            value_type_car_dict_key=#{valueTypeCarDictKey},
        </if>
        <if test="#{introduction}!=null">
            introduction=#{introduction},
        </if>
        where id=#{id}
    </update>

    <select id="getCarInforAttribById" resultType="course.linkflower.link.oneframework.vo.carinforattrib.CarInforAttribVo">
        select * from car_infor_attrib where id=#{id} limit 1
    </select>
</mapper>