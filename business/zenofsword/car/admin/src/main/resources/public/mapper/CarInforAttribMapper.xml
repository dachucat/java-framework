<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="course.linkflower.link.oneframework.dao.CarInforAttribMapper">
    <select id="countCarInforIdAttribKeyValue" resultType="int">
        select count(*) from car_infor_attrib where car_infor_id=#{carInforId} , attrib_key={attribKey} , value=#{value} limit 1
    </select>

    <insert id="add">
        insert into car_infor_attrib(car_infor_id, attrib_key, value, remark, icon, value_type_key, introduction)
        values(#{carInforId},#{attribKey},#{value},#{remark},#{icon},#{attribKey},#{introduction})
    </insert>

    <delete id="deleteById">
        delete from car_infor_attrib where id=#{id} limit 1
    </delete>

    <update id="updateById">
        update car_infor_attrib set
        <if test="carInforId!=null">
            car_infor_id=#{carInforId}
        </if>
        <if test="attribKey!=null">
            ,attrib_key=#{attribKey}
        </if>
        <if test="value!=null">
            ,value=#{value}
        </if>
        <if test="remark!=null">
            ,remark=#{remark}
        </if>
        <if test="icon!=null">
            ,icon=#{icon}
        </if>
        <if test="valueTypeKey!=null">
            ,value_type_key=#{valueTypeKey}
        </if>
        <if test="introduction!=null">
            ,introduction=#{introduction}
        </if>
        where id=#{id}
    </update>

    <select id="getCarInforAttribById" resultType="course.linkflower.link.oneframework.vo.carinforattrib.CarInforAttribVo">
        select * from car_infor_attrib where id=#{id} limit 1
    </select>

    <select id="countCarInforIdTypeKey" resultType="int">
        select count(*) from car_infor_attrib where car_infor_id=#{carInforId} and attrib_key=#{attribKey} limit 1
    </select>

    <select id="countCarInforIdAttribKeyValueDiffId" resultType="int">
        select count(*) from car_infor_attrib where car_infor_id=#{carInforId} and attrib_key=#{attribKey} and value=#{value} and id!=#{id} limit 1
    </select>

    <select id="listCarInforAttribByCarInforId" resultType="course.linkflower.link.oneframework.vo.carinforattrib.CarInforAttribVo">
        select * from car_infor_attrib where car_infor_id=#{id} limit #{defaultMaxCountLimit}
    </select>

    <select id="countCarInforIdAttribKey" resultType="int">
        select count(*) from car_infor_attrib where car_infor_id=#{carInforId} and attrib_key=#{attribKey} limit 1
    </select>
</mapper>